Alembic 数据库迁移
================

# 创建一份新迁移（基于当前 models.py 自动 diff）
uv run alembic revision --autogenerate -m "describe what changed"

# 应用所有未应用的迁移
uv run alembic upgrade head

# 查看当前版本
uv run alembic current

# 回滚一步
uv run alembic downgrade -1

# 查看迁移历史
uv run alembic history

约定
----
- 任何 schema 变更必须走 migration，不要直接改 models.py 后跑 create_all
- 自动生成的 migration 一定要人工 review，特别是删字段/改类型这类破坏性操作
- 提交前在本地 SQLite 跑通 upgrade + downgrade 一次
